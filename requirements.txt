# requirements.txt

torch>=2.0  # For model training, with GPU support
torchaudio>=2.0  # For working with audio data
transformers>=4.30.0  # For using the Wav2Vec2 model
datasets>=2.8.0  # For managing datasets, loading and preprocessing
scikit-learn>=1.2.0  # For diarization, clustering, and evaluation
pydub>=0.25.1  # For audio file processing
soundfile>=0.11.0  # For reading and writing audio files
librosa>=0.10.0  # For additional audio feature extraction
tensorboard>=2.10  # For logging training progress
numpy>=1.23.0  # General numerical computation
scipy>=1.10.0  # For signal processing and clustering tasks
tqdm>=4.64.0  # For progress bars during data loading and training
pytorch_lightning>=2.0  # Optional: Simplify training loops and GPU handling
